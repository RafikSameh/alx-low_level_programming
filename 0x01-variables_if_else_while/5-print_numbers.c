#include <stdio.h>
/**
 * main - is a function
 * flollowed by a new line
 * Return: 0 (success)
 */
int main(void)
{
	int n = 0;

	for (n = 0 ; n <= 10 ; n++)
	{
		printf("%d\n", n);
	}
	return (0);
}
